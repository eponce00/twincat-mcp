[
  {
    "id": "engineering.matching_login",
    "description": "Log into the unchanged baseline in this engineering context. Requires retained compile information, matching configuration/platform, configured ADS port and a RUN application. Rejects download/change prompts.",
    "kind": "operation",
    "command": "matching-login",
    "engineering": true,
    "requiresGrant": true,
    "readOnly": false,
    "inputSchema": {
      "type": "object",
      "properties": {
        "port": {
          "type": "integer",
          "minimum": 1,
          "maximum": 65535
        },
        "configuration": {
          "type": "string",
          "description": "Matching baseline configuration, e.g. Release",
          "minLength": 1
        },
        "platform": {
          "type": "string",
          "description": "Matching baseline platform, e.g. TwinCAT RT (x64)",
          "minLength": 1
        },
        "confirm": {
          "const": "CONFIRM",
          "type": "string",
          "minLength": 1
        },
        "contextHandle": {
          "type": "string",
          "minLength": 1,
          "description": "Exclusive engineering context returned by context.open."
        },
        "grantHandle": {
          "type": "string",
          "minLength": 1,
          "description": "Unexpired safety grant for this operation and target/context."
        }
      },
      "required": [
        "port",
        "configuration",
        "platform",
        "confirm",
        "contextHandle",
        "grantHandle"
      ],
      "additionalProperties": false
    }
  },
  {
    "id": "engineering.read_plc_source",
    "description": "Read a POU declaration or implementation and its exact UTF-8 SHA256 from the engineering context.",
    "kind": "operation",
    "command": "read-plc-source",
    "engineering": true,
    "requiresGrant": false,
    "readOnly": true,
    "inputSchema": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "minLength": 1
        },
        "section": {
          "type": "string",
          "enum": [
            "declaration",
            "implementation"
          ],
          "minLength": 1
        },
        "contextHandle": {
          "type": "string",
          "minLength": 1,
          "description": "Exclusive engineering context returned by context.open."
        }
      },
      "required": [
        "path",
        "section",
        "contextHandle"
      ],
      "additionalProperties": false
    }
  },
  {
    "id": "engineering.edit_plc_source",
    "description": "Edit and save one POU section only if expectedSha256 matches. Requires the existing logged-in engineering context. Does not compile or apply the edit.",
    "kind": "operation",
    "command": "edit-plc-source",
    "engineering": true,
    "requiresGrant": true,
    "readOnly": false,
    "inputSchema": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "minLength": 1
        },
        "section": {
          "type": "string",
          "enum": [
            "declaration",
            "implementation"
          ],
          "minLength": 1
        },
        "text": {
          "type": "string"
        },
        "expectedSha256": {
          "type": "string",
          "pattern": "^[a-fA-F0-9]{64}$",
          "minLength": 1
        },
        "contextHandle": {
          "type": "string",
          "minLength": 1,
          "description": "Exclusive engineering context returned by context.open."
        },
        "grantHandle": {
          "type": "string",
          "minLength": 1,
          "description": "Unexpired safety grant for this operation and target/context."
        }
      },
      "required": [
        "path",
        "section",
        "text",
        "expectedSha256",
        "contextHandle",
        "grantHandle"
      ],
      "additionalProperties": false
    }
  },
  {
    "id": "engineering.online_change",
    "description": "Apply one online change in the already logged-in matching PLC context. Verify one counter increment and advancing cycles. Never substitute download, activate, restart or boot-project update.",
    "kind": "operation",
    "command": "online-change",
    "engineering": true,
    "requiresGrant": true,
    "readOnly": false,
    "inputSchema": {
      "type": "object",
      "properties": {
        "port": {
          "type": "integer",
          "minimum": 1,
          "maximum": 65535
        },
        "cycleSymbol": {
          "type": "string",
          "description": "ULINT application cycle counter; must advance before and after dispatch.",
          "minLength": 1
        },
        "contextFile": {
          "type": "string",
          "description": "Existing absolute source file in the selected online PLC application; its editor establishes the XAE Online Change target context.",
          "minLength": 1
        },
        "expectedOnlineChangeCount": {
          "type": "integer",
          "minimum": 0,
          "maximum": 4294967295
        },
        "timeoutMs": {
          "type": "integer",
          "minimum": 100,
          "maximum": 60000
        },
        "confirm": {
          "const": "CONFIRM",
          "type": "string",
          "minLength": 1
        },
        "contextHandle": {
          "type": "string",
          "minLength": 1,
          "description": "Exclusive engineering context returned by context.open."
        },
        "grantHandle": {
          "type": "string",
          "minLength": 1,
          "description": "Unexpired safety grant for this operation and target/context."
        }
      },
      "required": [
        "port",
        "contextFile",
        "cycleSymbol",
        "expectedOnlineChangeCount",
        "confirm",
        "contextHandle",
        "grantHandle"
      ],
      "additionalProperties": false
    }
  },
  {
    "id": "engineering.build",
    "description": "Build the solution and return structured errors, warnings and timing. Set clean:false to preserve compile information for online change.",
    "kind": "operation",
    "command": "build",
    "engineering": true,
    "requiresGrant": false,
    "readOnly": false,
    "inputSchema": {
      "type": "object",
      "properties": {
        "clean": {
          "type": "boolean",
          "description": "Clean solution before building (default: true)",
          "default": true
        },
        "contextHandle": {
          "type": "string",
          "minLength": 1,
          "description": "Exclusive engineering context returned by context.open."
        }
      },
      "required": [
        "contextHandle"
      ],
      "additionalProperties": false
    }
  },
  {
    "id": "engineering.info",
    "description": "Read solution, toolchain and PLC project information in the engineering context.",
    "kind": "operation",
    "command": "info",
    "engineering": true,
    "requiresGrant": false,
    "readOnly": true,
    "inputSchema": {
      "type": "object",
      "properties": {
        "contextHandle": {
          "type": "string",
          "minLength": 1,
          "description": "Exclusive engineering context returned by context.open."
        }
      },
      "required": [
        "contextHandle"
      ],
      "additionalProperties": false
    }
  },
  {
    "id": "engineering.clean",
    "description": "Delete generated build artifacts from the solution. Invalidates retained compile information needed for online change.",
    "kind": "operation",
    "command": "clean",
    "engineering": true,
    "requiresGrant": false,
    "readOnly": false,
    "inputSchema": {
      "type": "object",
      "properties": {
        "contextHandle": {
          "type": "string",
          "minLength": 1,
          "description": "Exclusive engineering context returned by context.open."
        }
      },
      "required": [
        "contextHandle"
      ],
      "additionalProperties": false
    }
  },
  {
    "id": "engineering.set_target",
    "description": "Set the solution target to the AMS Net ID fixed by context.open. Does not activate.",
    "kind": "operation",
    "command": "set-target",
    "engineering": true,
    "requiresGrant": true,
    "readOnly": false,
    "inputSchema": {
      "type": "object",
      "properties": {
        "contextHandle": {
          "type": "string",
          "minLength": 1,
          "description": "Exclusive engineering context returned by context.open."
        },
        "grantHandle": {
          "type": "string",
          "minLength": 1,
          "description": "Unexpired safety grant for this operation and target/context."
        }
      },
      "required": [
        "contextHandle",
        "grantHandle"
      ],
      "additionalProperties": false
    }
  },
  {
    "id": "engineering.activate",
    "description": "Download and activate the configuration on the context target. Requires an operation-scoped grant and CONFIRM.",
    "kind": "operation",
    "command": "activate",
    "engineering": true,
    "requiresGrant": true,
    "readOnly": false,
    "inputSchema": {
      "type": "object",
      "properties": {
        "confirm": {
          "const": "CONFIRM",
          "type": "string",
          "minLength": 1
        },
        "contextHandle": {
          "type": "string",
          "minLength": 1,
          "description": "Exclusive engineering context returned by context.open."
        },
        "grantHandle": {
          "type": "string",
          "minLength": 1,
          "description": "Unexpired safety grant for this operation and target/context."
        }
      },
      "required": [
        "contextHandle",
        "grantHandle",
        "confirm"
      ],
      "additionalProperties": false
    }
  },
  {
    "id": "engineering.restart",
    "description": "Restart the TwinCAT runtime on the context target. Requires an operation-scoped grant and CONFIRM.",
    "kind": "operation",
    "command": "restart",
    "engineering": true,
    "requiresGrant": true,
    "readOnly": false,
    "inputSchema": {
      "type": "object",
      "properties": {
        "confirm": {
          "const": "CONFIRM",
          "type": "string",
          "minLength": 1
        },
        "contextHandle": {
          "type": "string",
          "minLength": 1,
          "description": "Exclusive engineering context returned by context.open."
        },
        "grantHandle": {
          "type": "string",
          "minLength": 1,
          "description": "Unexpired safety grant for this operation and target/context."
        }
      },
      "required": [
        "contextHandle",
        "grantHandle",
        "confirm"
      ],
      "additionalProperties": false
    }
  },
  {
    "id": "workflow.deploy",
    "description": "Build, prepare the boot project, activate configuration and restart the context target. Returns the native deployment receipt. dryRun previews the workflow.",
    "kind": "workflow",
    "command": "deploy",
    "engineering": true,
    "requiresGrant": true,
    "readOnly": false,
    "inputSchema": {
      "type": "object",
      "properties": {
        "skipBuild": {
          "type": "boolean",
          "description": "Skip building the solution (default: false)",
          "default": false
        },
        "dryRun": {
          "type": "boolean",
          "description": "Show what would be done without making changes (default: false)",
          "default": false
        },
        "confirm": {
          "const": "CONFIRM",
          "type": "string",
          "minLength": 1
        },
        "contextHandle": {
          "type": "string",
          "minLength": 1,
          "description": "Exclusive engineering context returned by context.open."
        },
        "grantHandle": {
          "type": "string",
          "minLength": 1,
          "description": "Unexpired safety grant for this operation and target/context."
        }
      },
      "required": [
        "contextHandle",
        "grantHandle",
        "confirm"
      ],
      "additionalProperties": false
    }
  },
  {
    "id": "engineering.list_plcs",
    "description": "List PLC projects, configured ADS ports and boot-project settings in the solution.",
    "kind": "operation",
    "command": "list-plcs",
    "engineering": true,
    "requiresGrant": false,
    "readOnly": true,
    "inputSchema": {
      "type": "object",
      "properties": {
        "contextHandle": {
          "type": "string",
          "minLength": 1,
          "description": "Exclusive engineering context returned by context.open."
        }
      },
      "required": [
        "contextHandle"
      ],
      "additionalProperties": false
    }
  },
  {
    "id": "engineering.set_boot_project",
    "description": "Configure autostart and generate the selected PLC boot project on the context target.",
    "kind": "operation",
    "command": "set-boot-project",
    "engineering": true,
    "requiresGrant": true,
    "readOnly": false,
    "inputSchema": {
      "type": "object",
      "properties": {
        "autostart": {
          "type": "boolean",
          "description": "Enable boot project autostart (default: true)",
          "default": true
        },
        "generate": {
          "type": "boolean",
          "description": "Generate boot project on target (default: true)",
          "default": true
        },
        "contextHandle": {
          "type": "string",
          "minLength": 1,
          "description": "Exclusive engineering context returned by context.open."
        },
        "grantHandle": {
          "type": "string",
          "minLength": 1,
          "description": "Unexpired safety grant for this operation and target/context."
        }
      },
      "required": [
        "contextHandle",
        "grantHandle"
      ],
      "additionalProperties": false
    }
  },
  {
    "id": "engineering.disable_io",
    "description": "Enable or disable top-level I/O devices in the solution configuration.",
    "kind": "operation",
    "command": "disable-io",
    "engineering": true,
    "requiresGrant": true,
    "readOnly": false,
    "inputSchema": {
      "type": "object",
      "properties": {
        "enable": {
          "type": "boolean",
          "description": "If true, enable I/O devices instead of disabling (default: false = disable)",
          "default": false
        },
        "contextHandle": {
          "type": "string",
          "minLength": 1,
          "description": "Exclusive engineering context returned by context.open."
        },
        "grantHandle": {
          "type": "string",
          "minLength": 1,
          "description": "Unexpired safety grant for this operation and target/context."
        }
      },
      "required": [
        "contextHandle",
        "grantHandle"
      ],
      "additionalProperties": false
    }
  },
  {
    "id": "engineering.set_variant",
    "description": "Get or set the solution variant. Requires TwinCAT XAE 4024 or newer; omit variantName to query.",
    "kind": "operation",
    "command": "set-variant",
    "engineering": true,
    "requiresGrant": true,
    "readOnly": false,
    "inputSchema": {
      "type": "object",
      "properties": {
        "variantName": {
          "type": "string",
          "description": "Name of the variant to set (e.g., 'PrimaryPLC'). Omit to just get current variant.",
          "minLength": 1
        },
        "contextHandle": {
          "type": "string",
          "minLength": 1,
          "description": "Exclusive engineering context returned by context.open."
        },
        "grantHandle": {
          "type": "string",
          "minLength": 1,
          "description": "Unexpired safety grant for this operation and target/context."
        }
      },
      "required": [
        "contextHandle",
        "grantHandle"
      ],
      "additionalProperties": false
    }
  },
  {
    "id": "runtime.list_symbols",
    "description": "Enumerate runtime symbols over ADS with prefix/substring filters and a bounded result count. includeTypes adds type and size.",
    "kind": "operation",
    "command": "list-symbols",
    "engineering": false,
    "requiresGrant": false,
    "readOnly": true,
    "inputSchema": {
      "type": "object",
      "properties": {
        "amsNetId": {
          "type": "string",
          "pattern": "^(?:\\d{1,3}\\.){5}\\d{1,3}$",
          "description": "Explicit target AMS Net ID; six octets, each 0..255.",
          "minLength": 1
        },
        "port": {
          "type": "integer",
          "description": "AMS port (default: 851 for PLC runtime 1)",
          "default": 851,
          "minimum": 1,
          "maximum": 65535
        },
        "prefix": {
          "type": "string",
          "description": "Case-insensitive prefix on the full symbol path (e.g., 'MAIN.')"
        },
        "contains": {
          "type": "string",
          "description": "Case-insensitive substring anywhere in the symbol path"
        },
        "max": {
          "type": "integer",
          "description": "Cap on returned entries (default: 200). TotalMatched is reported separately so you can widen if needed.",
          "default": 200,
          "minimum": 1,
          "maximum": 2000
        },
        "includeTypes": {
          "type": "boolean",
          "description": "Include type name + size per symbol (default: false; bump when you need the schema to write or cast)",
          "default": false
        }
      },
      "required": [
        "amsNetId"
      ],
      "additionalProperties": false
    }
  },
  {
    "id": "runtime.read_plc_log",
    "description": "Listen to the TwinCAT event log over ADS for a bounded window, optionally filtering messages. Requires the TcEventLogger proxy.",
    "kind": "operation",
    "command": "read-plc-log",
    "engineering": false,
    "requiresGrant": false,
    "readOnly": true,
    "inputSchema": {
      "type": "object",
      "properties": {
        "amsNetId": {
          "type": "string",
          "pattern": "^(?:\\d{1,3}\\.){5}\\d{1,3}$",
          "description": "Explicit target AMS Net ID; six octets, each 0..255.",
          "minLength": 1
        },
        "waitSeconds": {
          "type": "integer",
          "description": "Seconds to listen for new log events (default: 5). Reaches back only over this window, not historical.",
          "default": 5,
          "minimum": 0,
          "maximum": 60
        },
        "contains": {
          "type": "string",
          "description": "Case-insensitive substring filter on the message body"
        },
        "max": {
          "type": "integer",
          "description": "Cap on returned events (default: 200)",
          "default": 200,
          "minimum": 1,
          "maximum": 2000
        }
      },
      "required": [
        "amsNetId"
      ],
      "additionalProperties": false
    }
  },
  {
    "id": "runtime.ping_target",
    "description": "Probe system and runtime ADS reachability with per-probe timeoutMs; distinguish unreachable, rebooting and reachable targets.",
    "kind": "operation",
    "command": "ping-target",
    "engineering": false,
    "requiresGrant": false,
    "readOnly": true,
    "inputSchema": {
      "type": "object",
      "properties": {
        "amsNetId": {
          "type": "string",
          "pattern": "^(?:\\d{1,3}\\.){5}\\d{1,3}$",
          "description": "Explicit target AMS Net ID; six octets, each 0..255.",
          "minLength": 1
        },
        "port": {
          "type": "integer",
          "description": "PLC runtime AMS port (default: 851). Only the runtime probe uses this; the system-service probe always uses 10000.",
          "default": 851,
          "minimum": 1,
          "maximum": 65535
        },
        "timeoutMs": {
          "type": "integer",
          "description": "Per-probe timeout in milliseconds (default: 2500). Worst-case total = 2 \u00d7 timeoutMs.",
          "default": 2500
        }
      },
      "required": [
        "amsNetId"
      ],
      "additionalProperties": false
    }
  },
  {
    "id": "runtime.get_state",
    "description": "Read the explicit target runtime state over ADS without loading a solution.",
    "kind": "operation",
    "command": "get-state",
    "engineering": false,
    "requiresGrant": false,
    "readOnly": true,
    "inputSchema": {
      "type": "object",
      "properties": {
        "amsNetId": {
          "type": "string",
          "pattern": "^(?:\\d{1,3}\\.){5}\\d{1,3}$",
          "description": "Explicit target AMS Net ID; six octets, each 0..255.",
          "minLength": 1
        },
        "port": {
          "type": "integer",
          "description": "ADS port number (default: 851 for PLC runtime 1)",
          "default": 851,
          "minimum": 1,
          "maximum": 65535
        }
      },
      "required": [
        "amsNetId"
      ],
      "additionalProperties": false
    }
  },
  {
    "id": "runtime.set_state",
    "description": "Change the explicit target runtime state over ADS. Requires a grant for this operation and AMS target.",
    "kind": "operation",
    "command": "set-state",
    "engineering": false,
    "requiresGrant": true,
    "readOnly": false,
    "inputSchema": {
      "type": "object",
      "properties": {
        "amsNetId": {
          "type": "string",
          "pattern": "^(?:\\d{1,3}\\.){5}\\d{1,3}$",
          "description": "Explicit target AMS Net ID; six octets, each 0..255.",
          "minLength": 1
        },
        "state": {
          "type": "string",
          "description": "Target state: Run, Stop, Config, or Reset",
          "minLength": 1
        },
        "port": {
          "type": "integer",
          "description": "ADS port number (default: 851, auto-switches to 10000 for system state changes)",
          "default": 851,
          "minimum": 1,
          "maximum": 65535
        },
        "grantHandle": {
          "type": "string",
          "minLength": 1,
          "description": "Unexpired safety grant for this operation and target/context."
        }
      },
      "required": [
        "state",
        "amsNetId",
        "grantHandle"
      ],
      "additionalProperties": false
    }
  },
  {
    "id": "runtime.read_var",
    "description": "Read one PLC symbol over ADS, returning value, data type and byte size.",
    "kind": "operation",
    "command": "read-var",
    "engineering": false,
    "requiresGrant": false,
    "readOnly": true,
    "inputSchema": {
      "type": "object",
      "properties": {
        "amsNetId": {
          "type": "string",
          "pattern": "^(?:\\d{1,3}\\.){5}\\d{1,3}$",
          "description": "Explicit target AMS Net ID; six octets, each 0..255.",
          "minLength": 1
        },
        "symbol": {
          "type": "string",
          "description": "Full symbol path of the variable (e.g., 'MAIN.bMyBool', 'GVL.nCounter')",
          "minLength": 1
        },
        "port": {
          "type": "integer",
          "description": "ADS port number (default: 851 for PLC runtime 1)",
          "default": 851,
          "minimum": 1,
          "maximum": 65535
        }
      },
      "required": [
        "symbol",
        "amsNetId"
      ],
      "additionalProperties": false
    }
  },
  {
    "id": "runtime.write_var",
    "description": "Write one PLC symbol over ADS. Requires a grant for this operation and AMS target.",
    "kind": "operation",
    "command": "write-var",
    "engineering": false,
    "requiresGrant": true,
    "readOnly": false,
    "inputSchema": {
      "type": "object",
      "properties": {
        "amsNetId": {
          "type": "string",
          "pattern": "^(?:\\d{1,3}\\.){5}\\d{1,3}$",
          "description": "Explicit target AMS Net ID; six octets, each 0..255.",
          "minLength": 1
        },
        "symbol": {
          "type": "string",
          "description": "Full symbol path of the variable (e.g., 'MAIN.bMyBool', 'GVL.nCounter')",
          "minLength": 1
        },
        "value": {
          "type": "string",
          "description": "Value to write (will be converted to appropriate type). Examples: 'true', '42', '3.14', 'Hello'",
          "minLength": 1
        },
        "port": {
          "type": "integer",
          "description": "ADS port number (default: 851 for PLC runtime 1)",
          "default": 851,
          "minimum": 1,
          "maximum": 65535
        },
        "grantHandle": {
          "type": "string",
          "minLength": 1,
          "description": "Unexpired safety grant for this operation and target/context."
        }
      },
      "required": [
        "symbol",
        "value",
        "amsNetId",
        "grantHandle"
      ],
      "additionalProperties": false
    }
  },
  {
    "id": "engineering.list_tasks",
    "description": "List configured real-time tasks with cycle times, priorities and enabled state.",
    "kind": "operation",
    "command": "list-tasks",
    "engineering": true,
    "requiresGrant": false,
    "readOnly": true,
    "inputSchema": {
      "type": "object",
      "properties": {
        "contextHandle": {
          "type": "string",
          "minLength": 1,
          "description": "Exclusive engineering context returned by context.open."
        }
      },
      "required": [
        "contextHandle"
      ],
      "additionalProperties": false
    }
  },
  {
    "id": "engineering.configure_task",
    "description": "Enable or disable a named real-time task or configure its autostart setting.",
    "kind": "operation",
    "command": "configure-task",
    "engineering": true,
    "requiresGrant": true,
    "readOnly": false,
    "inputSchema": {
      "type": "object",
      "properties": {
        "taskName": {
          "type": "string",
          "description": "Name of the task to configure (e.g., 'PlcTask', 'TestTask')",
          "minLength": 1
        },
        "enable": {
          "type": "boolean",
          "description": "If true, enable the task. If false, disable the task. Optional."
        },
        "autostart": {
          "type": "boolean",
          "description": "If true, task starts automatically on activation. If false, requires manual start. Optional."
        },
        "contextHandle": {
          "type": "string",
          "minLength": 1,
          "description": "Exclusive engineering context returned by context.open."
        },
        "grantHandle": {
          "type": "string",
          "minLength": 1,
          "description": "Unexpired safety grant for this operation and target/context."
        }
      },
      "required": [
        "taskName",
        "contextHandle",
        "grantHandle"
      ],
      "additionalProperties": false
    }
  },
  {
    "id": "engineering.configure_rt",
    "description": "Configure isolated real-time CPU count and load limit in the solution.",
    "kind": "operation",
    "command": "configure-rt",
    "engineering": true,
    "requiresGrant": true,
    "readOnly": false,
    "inputSchema": {
      "type": "object",
      "properties": {
        "maxCpus": {
          "type": "integer",
          "description": "Maximum number of CPU cores for isolated real-time cores (1-based). Default: 1",
          "minimum": 1,
          "maximum": 256
        },
        "loadLimit": {
          "type": "integer",
          "description": "CPU load limit percentage (1-100). Default: 50",
          "minimum": 1,
          "maximum": 100
        },
        "contextHandle": {
          "type": "string",
          "minLength": 1,
          "description": "Exclusive engineering context returned by context.open."
        },
        "grantHandle": {
          "type": "string",
          "minLength": 1,
          "description": "Unexpired safety grant for this operation and target/context."
        }
      },
      "required": [
        "contextHandle",
        "grantHandle"
      ],
      "additionalProperties": false
    }
  },
  {
    "id": "engineering.check_all_objects",
    "description": "Compile all PLC objects, including unused objects, and return structured diagnostics.",
    "kind": "operation",
    "command": "check-all-objects",
    "engineering": true,
    "requiresGrant": false,
    "readOnly": false,
    "inputSchema": {
      "type": "object",
      "properties": {
        "contextHandle": {
          "type": "string",
          "minLength": 1,
          "description": "Exclusive engineering context returned by context.open."
        }
      },
      "required": [
        "contextHandle"
      ],
      "additionalProperties": false
    }
  },
  {
    "id": "engineering.static_analysis",
    "description": "Run TE1200 static analysis and return rule violations. Requires the appropriate license.",
    "kind": "operation",
    "command": "static-analysis",
    "engineering": true,
    "requiresGrant": false,
    "readOnly": false,
    "inputSchema": {
      "type": "object",
      "properties": {
        "checkAll": {
          "type": "boolean",
          "description": "Check all objects including unused ones (default: true)",
          "default": true
        },
        "contextHandle": {
          "type": "string",
          "minLength": 1,
          "description": "Exclusive engineering context returned by context.open."
        }
      },
      "required": [
        "contextHandle"
      ],
      "additionalProperties": false
    }
  },
  {
    "id": "engineering.generate_library",
    "description": "Build and export the context PLC as a .library artifact. Existing outputs are backed up; dryRun previews the export.",
    "kind": "operation",
    "command": "generate-library",
    "engineering": true,
    "requiresGrant": false,
    "readOnly": false,
    "inputSchema": {
      "type": "object",
      "properties": {
        "libraryLocation": {
          "type": "string",
          "description": "Optional output directory or explicit .library file path",
          "minLength": 1
        },
        "skipBuild": {
          "type": "boolean",
          "description": "Skip build before export (default: false)",
          "default": false
        },
        "dryRun": {
          "type": "boolean",
          "description": "Validate flow without exporting (default: false)",
          "default": false
        },
        "contextHandle": {
          "type": "string",
          "minLength": 1,
          "description": "Exclusive engineering context returned by context.open."
        }
      },
      "required": [
        "contextHandle"
      ],
      "additionalProperties": false
    }
  },
  {
    "id": "system.routes",
    "description": "List configured ADS routes through Beckhoff TcXaeMgmt, including Secure ADS mode, certificate fingerprint and current online status.",
    "kind": "operation",
    "command": "list-routes",
    "engineering": false,
    "requiresGrant": false,
    "readOnly": true,
    "inputSchema": {
      "type": "object",
      "properties": {},
      "required": [],
      "additionalProperties": false
    }
  },
  {
    "id": "system.route_upsert",
    "description": "Create or replace one fingerprint-verified self-signed Secure ADS route using Beckhoff TcXaeMgmt, then verify the target ADS port. Reads the target credential from a DPAPI-protected PSCredential file; plaintext passwords are not accepted.",
    "kind": "operation",
    "command": "route-upsert",
    "engineering": false,
    "requiresGrant": true,
    "readOnly": false,
    "inputSchema": {
      "type": "object",
      "properties": {
        "amsNetId": {
          "type": "string",
          "pattern": "^(?:\\d{1,3}\\.){5}\\d{1,3}$",
          "description": "Explicit target AMS Net ID; six octets, each 0..255.",
          "minLength": 1
        },
        "ipOrHostName": {
          "type": "string",
          "minLength": 1,
          "maxLength": 255
        },
        "name": {
          "type": "string",
          "minLength": 1,
          "maxLength": 255
        },
        "credentialPath": {
          "type": "string",
          "minLength": 1,
          "description": "Absolute path to a DPAPI-protected PSCredential created by Export-Clixml for the current OS user."
        },
        "fingerprint": {
          "type": "string",
          "pattern": "^[a-fA-F0-9]{64}$",
          "description": "Expected SHA-256 fingerprint of the target self-signed certificate."
        },
        "verifyPort": {
          "type": "integer",
          "minimum": 1,
          "maximum": 65535,
          "default": 10000
        },
        "unidirectional": {
          "type": "boolean",
          "default": false
        },
        "confirm": {
          "const": "CONFIRM",
          "type": "string",
          "minLength": 1
        },
        "grantHandle": {
          "type": "string",
          "minLength": 1,
          "description": "Unexpired safety grant for this operation and target/context."
        }
      },
      "required": [
        "amsNetId",
        "ipOrHostName",
        "name",
        "credentialPath",
        "fingerprint",
        "confirm",
        "grantHandle"
      ],
      "additionalProperties": false
    }
  },
  {
    "id": "system.route_remove",
    "description": "Remove one exact ADS route locally, or from both systems when removeRemote is true. Remote removal requires a DPAPI-protected PSCredential file.",
    "kind": "operation",
    "command": "route-remove",
    "engineering": false,
    "requiresGrant": true,
    "readOnly": false,
    "inputSchema": {
      "type": "object",
      "properties": {
        "amsNetId": {
          "type": "string",
          "pattern": "^(?:\\d{1,3}\\.){5}\\d{1,3}$",
          "description": "Explicit target AMS Net ID; six octets, each 0..255.",
          "minLength": 1
        },
        "removeRemote": {
          "type": "boolean",
          "default": false
        },
        "credentialPath": {
          "type": "string",
          "minLength": 1,
          "description": "Required only when removeRemote is true; absolute path to a DPAPI-protected PSCredential."
        },
        "confirm": {
          "const": "CONFIRM",
          "type": "string",
          "minLength": 1
        },
        "grantHandle": {
          "type": "string",
          "minLength": 1,
          "description": "Unexpired safety grant for this operation and target/context."
        }
      },
      "required": [
        "amsNetId",
        "confirm",
        "grantHandle"
      ],
      "additionalProperties": false
    }
  },
  {
    "id": "engineering.get_error_list",
    "description": "Read the XAE Error List with severity and substring filters; waitSeconds permits a bounded delay for async messages.",
    "kind": "operation",
    "command": "get-error-list",
    "engineering": true,
    "requiresGrant": false,
    "readOnly": true,
    "inputSchema": {
      "type": "object",
      "properties": {
        "includeMessages": {
          "type": "boolean",
          "description": "Include messages (ADS logs, TcUnit output, etc.). Default: true",
          "default": true
        },
        "includeWarnings": {
          "type": "boolean",
          "description": "Include warnings. Default: true",
          "default": true
        },
        "includeErrors": {
          "type": "boolean",
          "description": "Include errors. Default: true",
          "default": true
        },
        "waitSeconds": {
          "type": "integer",
          "description": "Wait N seconds before reading so async ADS messages have time to arrive. Default: 2.",
          "default": 2,
          "minimum": 0,
          "maximum": 60
        },
        "contains": {
          "type": "string",
          "description": "Case-insensitive substring filter applied to each item's description. Use this to cut through library-reload noise \u2014 e.g. 'FAILED TEST', 'E_SM_Fault', 'stack'. Omit to get everything (may be hundreds of items)."
        },
        "contextHandle": {
          "type": "string",
          "minLength": 1,
          "description": "Exclusive engineering context returned by context.open."
        }
      },
      "required": [
        "contextHandle"
      ],
      "additionalProperties": false
    }
  },
  {
    "id": "workflow.test",
    "description": "Build, configure the test task and boot project, optionally disable I/O, activate, restart and poll TcUnit results on the context target. Requires a grant even for local targets.",
    "kind": "workflow",
    "command": "run-tcunit",
    "engineering": true,
    "requiresGrant": true,
    "readOnly": false,
    "inputSchema": {
      "type": "object",
      "properties": {
        "taskName": {
          "type": "string",
          "description": "Name of the task running TcUnit tests (auto-detected if only one task)",
          "minLength": 1
        },
        "timeoutMinutes": {
          "type": "integer",
          "description": "Timeout in minutes (default: 10)",
          "default": 10,
          "minimum": 1,
          "maximum": 60
        },
        "disableIo": {
          "type": "boolean",
          "description": "Disable I/O devices for running without hardware (default: false)",
          "default": false
        },
        "skipBuild": {
          "type": "boolean",
          "description": "Skip building the solution (default: false)",
          "default": false
        },
        "contextHandle": {
          "type": "string",
          "minLength": 1,
          "description": "Exclusive engineering context returned by context.open."
        },
        "grantHandle": {
          "type": "string",
          "minLength": 1,
          "description": "Unexpired safety grant for this operation and target/context."
        },
        "confirm": {
          "type": "string",
          "const": "CONFIRM"
        }
      },
      "required": [
        "contextHandle",
        "grantHandle",
        "confirm"
      ],
      "additionalProperties": false
    }
  },
  {
    "id": "runtime.read_var_list",
    "description": "Read up to 500 PLC symbols in a single ADS batch and return structured values.",
    "kind": "operation",
    "command": "read-var-list",
    "engineering": false,
    "requiresGrant": false,
    "readOnly": true,
    "inputSchema": {
      "type": "object",
      "properties": {
        "amsNetId": {
          "type": "string",
          "pattern": "^(?:\\d{1,3}\\.){5}\\d{1,3}$",
          "description": "Explicit target AMS Net ID; six octets, each 0..255.",
          "minLength": 1
        },
        "symbols": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Array of symbol paths to read (e.g., ['GVL.nCounter', 'MAIN.bRunning']). Max 500.",
          "minItems": 1,
          "maxItems": 500
        },
        "port": {
          "type": "integer",
          "description": "ADS port number (default: 851)",
          "default": 851,
          "minimum": 1,
          "maximum": 65535
        }
      },
      "required": [
        "amsNetId",
        "symbols"
      ],
      "additionalProperties": false
    }
  },
  {
    "id": "runtime.write_var_list",
    "description": "Write up to 500 PLC symbol/value pairs in a single ADS batch. Requires a grant for this operation and target.",
    "kind": "operation",
    "command": "write-var-list",
    "engineering": false,
    "requiresGrant": true,
    "readOnly": false,
    "inputSchema": {
      "type": "object",
      "properties": {
        "amsNetId": {
          "type": "string",
          "pattern": "^(?:\\d{1,3}\\.){5}\\d{1,3}$",
          "description": "Explicit target AMS Net ID; six octets, each 0..255.",
          "minLength": 1
        },
        "variables": {
          "type": "object",
          "description": "Dictionary of symbol paths to values (e.g., {'GVL.x': '42', 'GVL.y': '3.14'}). Max 500 entries.",
          "additionalProperties": {
            "type": "string"
          },
          "minProperties": 1,
          "maxProperties": 500
        },
        "port": {
          "type": "integer",
          "description": "ADS port number (default: 851)",
          "default": 851,
          "minimum": 1,
          "maximum": 65535
        },
        "grantHandle": {
          "type": "string",
          "minLength": 1,
          "description": "Unexpired safety grant for this operation and target/context."
        }
      },
      "required": [
        "amsNetId",
        "variables",
        "grantHandle"
      ],
      "additionalProperties": false
    }
  },
  {
    "id": "runtime.record",
    "description": "Record PLC variables through ADS notifications into CSV with optional start/stop triggers. No TE13xx license is needed; recording and trigger waits are bounded by arguments.",
    "kind": "operation",
    "command": "ads-record",
    "engineering": false,
    "requiresGrant": false,
    "readOnly": false,
    "inputSchema": {
      "type": "object",
      "properties": {
        "amsNetId": {
          "type": "string",
          "pattern": "^(?:\\d{1,3}\\.){5}\\d{1,3}$",
          "description": "Explicit target AMS Net ID; six octets, each 0..255.",
          "minLength": 1
        },
        "port": {
          "type": "integer",
          "description": "ADS port number (default: 851 for PLC, 501 for NC)",
          "default": 851,
          "minimum": 1,
          "maximum": 65535
        },
        "variables": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "List of PLC variable paths to record (e.g., ['GVL.fSpeed', 'GVL.fPosition'])",
          "minItems": 1,
          "maxItems": 500
        },
        "sampleTimeMs": {
          "type": "integer",
          "description": "Sample interval in milliseconds (min: 1, default: 10)",
          "default": 10,
          "minimum": 1,
          "maximum": 60000
        },
        "durationSec": {
          "type": "number",
          "description": "Recording duration in seconds (0 = use maxTimeSec only). Optional when using triggers.",
          "default": 0,
          "minimum": 0,
          "maximum": 3600
        },
        "outputPath": {
          "type": "string",
          "description": "Optional path for the CSV output file. Default: auto-generated in temp folder.",
          "minLength": 1
        },
        "startTrigger": {
          "type": "string",
          "description": "Start recording when this condition is met. Format: 'VariablePath operator value' (e.g. 'MAIN.bRunning == 1'). Operators: > < >= <= == !=",
          "minLength": 1
        },
        "stopTrigger": {
          "type": "string",
          "description": "Stop recording when this condition is met. Format: 'VariablePath operator value'. Operators: > < >= <= == !=",
          "minLength": 1
        },
        "maxTimeSec": {
          "type": "number",
          "description": "Max seconds to wait for startTrigger, and safety fallback for open-ended recordings. Does NOT cap a timed durationSec recording. Default: 60",
          "default": 60,
          "minimum": 1,
          "maximum": 3600
        }
      },
      "required": [
        "amsNetId",
        "variables"
      ],
      "additionalProperties": false
    }
  },
  {
    "id": "scope.create_config",
    "description": "Create a .tcscopex file from explicit target, variables and recording settings. Requires a TE13xx-enabled worker.",
    "kind": "operation",
    "command": "scope-create-config",
    "engineering": false,
    "requiresGrant": false,
    "readOnly": false,
    "inputSchema": {
      "type": "object",
      "properties": {
        "amsNetId": {
          "type": "string",
          "pattern": "^(?:\\d{1,3}\\.){5}\\d{1,3}$",
          "description": "Explicit target AMS Net ID; six octets, each 0..255.",
          "minLength": 1
        },
        "port": {
          "type": "integer",
          "description": "ADS port number (default: 851 for PLC runtime 1)",
          "default": 851,
          "minimum": 1,
          "maximum": 65535
        },
        "variables": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "List of PLC variable paths to record (e.g., ['GVL.fMotorSpeed', 'GVL.fTemperature'])",
          "minItems": 1,
          "maxItems": 500
        },
        "sampleTimeMs": {
          "type": "integer",
          "description": "Sample interval in milliseconds (default: 10)",
          "default": 10,
          "minimum": 1,
          "maximum": 60000
        },
        "recordTimeSec": {
          "type": "number",
          "description": "Optional max recording duration in seconds. Omit for manual stop.",
          "minimum": 0,
          "maximum": 3600
        },
        "outputPath": {
          "type": "string",
          "description": "Optional path to save the .tcscopex file. Default: auto-generated in temp folder.",
          "minLength": 1
        },
        "chartName": {
          "type": "string",
          "description": "Optional display name for the chart (default: 'MCP Trace')",
          "minLength": 1
        }
      },
      "required": [
        "amsNetId",
        "variables"
      ],
      "additionalProperties": false
    }
  },
  {
    "id": "scope.start",
    "description": "Start a Scope Server recording from an existing .tcscopex file. Requires a grant scoped to that configPath; returns recordingHandle on success.",
    "kind": "operation",
    "command": "scope-start-record",
    "engineering": false,
    "requiresGrant": true,
    "readOnly": false,
    "inputSchema": {
      "type": "object",
      "properties": {
        "configPath": {
          "type": "string",
          "description": "Path to the .tcscopex configuration file (from scope.create_config)",
          "minLength": 1
        },
        "grantHandle": {
          "type": "string",
          "minLength": 1,
          "description": "Unexpired safety grant for this operation and target/context."
        }
      },
      "required": [
        "configPath",
        "grantHandle"
      ],
      "additionalProperties": false
    }
  },
  {
    "id": "scope.stop",
    "description": "Stop the recording identified by recordingHandle and export its data. Returns path, elapsed time and sample count.",
    "kind": "operation",
    "command": "scope-stop-record",
    "engineering": false,
    "requiresGrant": false,
    "readOnly": false,
    "inputSchema": {
      "type": "object",
      "properties": {
        "outputPath": {
          "type": "string",
          "description": "Optional path for the CSV output file. Default: auto-generated.",
          "minLength": 1
        },
        "format": {
          "type": "string",
          "enum": [
            "csv",
            "json"
          ],
          "description": "Output format (default: csv)",
          "default": "csv",
          "minLength": 1
        },
        "recordingHandle": {
          "type": "string",
          "minLength": 1
        }
      },
      "required": [
        "recordingHandle"
      ],
      "additionalProperties": false
    }
  },
  {
    "id": "scope.status",
    "description": "Read the status of the recording identified by recordingHandle. Never creates a new Scope process.",
    "kind": "operation",
    "command": "scope-get-status",
    "engineering": false,
    "requiresGrant": false,
    "readOnly": true,
    "inputSchema": {
      "type": "object",
      "properties": {
        "recordingHandle": {
          "type": "string",
          "minLength": 1
        }
      },
      "required": [
        "recordingHandle"
      ],
      "additionalProperties": false
    }
  },
  {
    "id": "scope.export",
    "description": "Export an existing Scope recording through TC3ScopeExportTool. Requires TE13xx; writes the requested output file.",
    "kind": "operation",
    "command": "scope-export",
    "engineering": false,
    "requiresGrant": false,
    "readOnly": false,
    "inputSchema": {
      "type": "object",
      "properties": {
        "inputPath": {
          "type": "string",
          "description": "Path to the .svdx or .tcscopex file to export",
          "minLength": 1
        },
        "outputPath": {
          "type": "string",
          "description": "Optional output path for the CSV. Default: same name with .csv extension.",
          "minLength": 1
        },
        "format": {
          "type": "string",
          "enum": [
            "csv",
            "tdms"
          ],
          "description": "Export format (default: csv)",
          "default": "csv",
          "minLength": 1
        }
      },
      "required": [
        "inputPath"
      ],
      "additionalProperties": false
    }
  }
]
